package com.syntax.review2;

public class IfStatement {
    public static void main(String[] args) {
        // if statement is a conditional statement // condition can be true or false // it's also a boolean condition

        int day=2;
        if(day==1){
            System.out.println("It is the first day of the month"); // this is a block of code
        }
        // = is used to assign a value. == is used as a relational operator. used to compare 2 things or numbers

        String month="December";
    if(month.equals("December"));{
        System.out.println("It is Christmas time");}


        boolean evening=true; // if you work with a boolean variable for 'if' condition, you can use the
                              // boolean variable as your if condition.
        if(evening){
            System.out.println(" I might have a class at Syntax");
        }
    }
}
